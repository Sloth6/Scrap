extends layout

block scripts
  script(src='/js/jquery/jquery.js')
  script(src="/js/jquery/jquery-ui.min.js")
  script(src="/js/jquery/jquery.transit.min.js")
  script(src="/js/jquery/jquery-mousewheel.min.js")
  //- script(src="/js/home/main.js")
  script(src="/js/home/headerMenu.js")
  script(src="/js/home/settingsMenu.js")


block content
  header.tempForm
    .card.form
      include partials/registration
  section.index
    header
      h1 Hotpot
      h2 Keep everything for your<br>project in one place.
      ul.form
        li Sign Up
        li Log In
    
    section.space
      section.content
    
        -var collection_height = 700
        -for (var i = 0; i < collections.length; i++)
          -var collection = collections[i]
          -var top = (i * collection_height)
          section.example
            header
              h1!=collection.name
            .collection(style="top:"+top+"px;height:"+(collection_height)+"px;")
              -for (var j = 0; j < collection.elements.length; j++)
                -var element = collection.elements[j]
                include partials/element
        script.
          collection_max_y="#{top}";

      //- section.example
      //-   header
      //-     h1 Hotpot is for Baking
      //-   section.collection
      //- section.example
      //-   header
      //-     h1 Hotpot is for Creperies
      //-   section.collection
      //- section.example
      //-   header
      //-     h1 Hotpot is for Dolphin Training
      //-   section.collection
      //- section.example
      //-   header
      //-     h1 Hotpot is for Entymology
      //-   section.collection
      //- section.example
      //-   header
      //-     h1 Hotpot is for Faberge Egg Collecting
      //-   section.collection
