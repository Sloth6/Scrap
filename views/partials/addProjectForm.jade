- var coverClasses = [ 'cover', 'closed', 'slider', 'sliding', 'editing', 'addProjectForm', 'element' ]
- var defaulttext = "New Project"
article(data-contenttype='addProjectForm', data-defaulttext=defaulttext, class=coverClasses)
  .card.newCard()
    section.title
      h1.collectionTitle.typeTitle.typeOutlineClear!=defaulttext
      h2.peekTitle.bodyType.typeBody.typeWeightBold!=defaulttext
      //- p.rename
      //-   a.smallType Create